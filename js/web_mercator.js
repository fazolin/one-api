// webMercatorConverter.js

// function convertToWebMercator(longitude, latitude, zoom) {
//   lon = radians(lon);
//   let a = (320 / PI) * pow(2, zoom);
//   let b = lon + PI;
//   let c = tan(PI / 4 + lat / 2);
//   let d = PI - log(c);
//   return {
//     x: a * b,
//     y: a * d,
//   };
// }
